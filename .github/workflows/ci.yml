name: Python checks
on:
  push:
    branches: [main]
  pull_request:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: python -m pip install -r requirements.txt
      - run: python -m pytest -q
      - run: python -m digits_ml train --output-dir artifacts
      - run: python -m digits_ml predict --model artifacts/model.joblib --input examples/digit.json
      - uses: actions/upload-artifact@v4
        with:
          name: benchmark-results
          path: |
            artifacts/metrics.json
            artifacts/confusion-matrix.png
